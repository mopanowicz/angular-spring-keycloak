export const environment = {
  production: false,
  // You can set these values at runtime by injecting a small script into index.html
  KEYCLOAK_URL: 'https://keycloak.example.com/auth',
  KEYCLOAK_REALM: 'myrealm',
  KEYCLOAK_CLIENT: 'angular-client'
};
